module.exports = Backbone.Model.extend({
    // Best practice: name all properties here even if you don't
    // have defaults for them.
    defaults: {
        name: 'mystery meat',
        price: 4.99,
    },
});